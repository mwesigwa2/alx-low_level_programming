#include "main.h"
/**
 * add - adds 2 integers and returns result
 * @x: integer 1
 * @y: integer 2
 * Return: sum of the two
 *
 */
int add(int x, int y)
{
	int sum;

	sum = (x + y);
	return (sum);
}
