#include "main.h"
/**
 * _isdigit - checks for a digit (0 through 9)
 *
 * @c: Character to be checked
 * Returns: 1 if c is a digit else 0
 *
 */
int _isdigit(int c)
{
	int i, j;

	for (i = 0; i <= 9, i++)
	{
		if (i
	}
}
